# Pin the project name so it does not vary with the invocation directory.
name: t1

services:
  # Started on demand by comfyui-proxy.socket, not by docker. ComfyUI holds
  # ~498 MiB of VRAM for its CUDA context with no model loaded, which is worth
  # reclaiming on a box where llama-server takes 29 of the 5090's 32 GB.
  comfyui:
    container_name: comfyui
    image: mmartial/comfyui-nvidia-docker:ubuntu24_cuda13.1-latest
    # Not unless-stopped: docker would start it at boot and hold the VRAM before
    # anyone asks for it, and would fight the idle stop by restarting it.
    restart: "no"
    # Loopback only. systemd owns the LAN-facing 8188 and proxies to this.
    ports:
      - "127.0.0.1:8189:8188"
    volumes:
      - ~/srv/comfyui:/comfy/mnt
      - ~/models:/host_models
    environment:
      - WANTED_UID=1000
      - WANTED_GID=1000
      - USE_UV=true
      - FORCE_CHOWN=true
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

  transmission:
    container_name: transmission
    image: linuxserver/transmission:4.0.5
    restart: unless-stopped
    network_mode: host
    volumes:
      - ~/srv/transmission/config:/config
      - ~/downloads:/downloads
      - ~/media:/media
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Europe/London
      - USER=${TRANSMISSION_USER}
      - PASS=${TRANSMISSION_PASS}

  jellyfin:
    container_name: jellyfin
    image: linuxserver/jellyfin:latest
    restart: unless-stopped
    ports:
      - "8096:8096"
    volumes:
      - ~/srv/jellyfin:/config
      - ~/media:/data/media
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Europe/London
      - NVIDIA_VISIBLE_DEVICES=all
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

  # yt-dlp proof-of-origin token provider, used by the Jellyfin YouTube library.
  bgutil-pot:
    container_name: bgutil-pot
    image: brainicism/bgutil-ytdlp-pot-provider:latest
    restart: unless-stopped
    ports:
      - "127.0.0.1:4416:4416"

  # MQTT broker for t1's own health metrics (see t1-metrics.service). Bound to
  # loopback: the publisher runs on the host and Home Assistant is host-networked,
  # so nothing crosses the LAN and no ufw rule is needed.
  mosquitto:
    container_name: mosquitto
    image: eclipse-mosquitto:2
    restart: unless-stopped
    ports:
      - "127.0.0.1:1883:1883"
    volumes:
      - ~/.dotfiles/sys/t1/mosquitto.conf:/mosquitto/config/mosquitto.conf:ro
      - ~/srv/mosquitto/passwd:/mosquitto/config/passwd:ro
      - ~/srv/mosquitto/data:/mosquitto/data
    environment:
      - TZ=Europe/London

  # host networking is required: HomeKit pairing and the HomeKit Bridge both need
  # mDNS, and a published port cannot carry multicast. Exposure is gated by ufw.
  homeassistant:
    container_name: homeassistant
    image: ghcr.io/home-assistant/home-assistant:stable
    restart: unless-stopped
    network_mode: host
    volumes:
      - ~/srv/homeassistant:/config
      - /run/dbus:/run/dbus:ro
      - /etc/localtime:/etc/localtime:ro
    # HA auto-discovers hci0 whether or not we intend to use Bluetooth, and
    # without dbus and these caps it retries a failing scanner forever. Granting
    # them silences that and keeps the BLE path available as a fallback.
    cap_add:
      - NET_ADMIN
      - NET_RAW
    environment:
      - TZ=Europe/London

  aitoolkit:
    container_name: aitoolkit
    image: ostris/aitoolkit:latest
    restart: unless-stopped
    shm_size: "16gb"
    ports:
      - "8675:8675"
    volumes:
      - ~/srv/aitoolkit/hf-cache:/root/.cache/huggingface
      - ~/srv/aitoolkit/aitk_db.db:/app/ai-toolkit/aitk_db.db
      - ~/srv/aitoolkit/datasets:/app/ai-toolkit/datasets
      - ~/srv/aitoolkit/output:/app/ai-toolkit/output
      - ~/srv/aitoolkit/config:/app/ai-toolkit/config
    environment:
      - NODE_ENV=production
      - TZ=Europe/London
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
