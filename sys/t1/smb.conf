[global]
    server string = t1
    bind interfaces only = yes
    interfaces = lo enp7s0 tailscale0
    map to guest = Never
    server min protocol = SMB2
    server role = standalone server

[videos]
    path = /mnt/aux/videos
    read only = yes
    guest ok = no
    valid users = shared
